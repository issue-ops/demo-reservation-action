/** Allowed Activities for the Action */
export enum AllowedActions {
  /** Initialize the Request */
  INIT = 'init',
  /** Submit the Request */
  SUBMIT = 'submit',
  /** Cancel the Request */
  CANCEL = 'cancel',
  /** Expire the Request */
  EXPIRE = 'expire'
}
